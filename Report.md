# 咖啡冲煮助手 · 开发报告

> 微信小程序 — 私人咖啡冲煮计时器  
> 版本: 1.0.0  
> 日期: 2026-06-25

---

## 一、项目概述

「咖啡冲煮助手」是一款面向手冲咖啡爱好者的微信小程序。用户可以在配置页设定冲煮参数（品种、烘焙度、研磨度、粉量等），系统自动计算最佳水温与粉水比；点击"开始冲煮"后进入计时模式，根据用户选定的注水手法（三段式/一刀流/十段法）动态展示当前阶段、时间范围、目标注水量和操作提示，并在阶段切换时触发震动提醒。

---

## 二、技术栈

| 层面 | 技术 |
|------|------|
| 小程序框架 | 原生微信小程序 (glass-easel) |
| 语言 | TypeScript + WXML + WXSS |
| 编译 | 微信开发者工具 TypeScript 编译插件 |
| 样式方案 | Flex 布局，CSS3 渐变/阴影/过渡 |
| API 依赖 | `wx.vibrateShort`、`wx.setKeepScreenOn`、`setInterval` |

---

## 三、文件清单

```
miniprogram/
├── app.json                        # 应用配置
├── app.ts                          # 应用入口
├── app.wxss                        # 全局样式
├── pages/
│   └── index/
│       ├── index.ts                # 核心业务逻辑（357 行）
│       ├── index.wxml              # 双视图模板（175 行）
│       ├── index.wxss              # 咖啡色系样式（454 行）
│       └── index.json              # 页面配置
└── utils/
    └── util.ts                     # 工具函数（保留，未改动）
```

---

## 四、架构设计

### 4.1 页面状态机

```
┌─────────────────────────┐
│    参数配置视图           │
│  (isBrewing = false)     │
│                         │
│  ┌───────────────────┐  │
│  │ 卡片1: 冲煮参数    │  │    点击「开始冲煮」
│  │ 卡片2: 计算结果    │  │  ───────────────────►
│  │ 卡片3: 注水手法    │  │
│  └───────────────────┘  │
└─────────────────────────┘
                                ┌─────────────────────────┐
                                │    冲煮计时视图           │
                                │  (isBrewing = true)      │
                                │                         │
                                │  ┌───────────────────┐  │
                                │  │ 大字计时器          │  │
                                │  │ 当前阶段卡片        │  │    点击「结束冲煮」
                                │  │ 阶段概览列表        │  │  ◄───────────────────
                                │  └───────────────────┘  │
                                └─────────────────────────┘
```

### 4.2 数据流

```
用户输入                      系统计算                        UI 输出
───────                      ────────                        ───────
methodIndex ─┐
roastIndex ──┼──► recalculate() ──► temperature ──────► 水温显示
weight ──────┘                     ratio ─────────────► 粉水比显示
                                   totalWater ────────► 总水量显示
                                       │
pourStyleIndex ────► recalculateStages() ──► stages[] ─► 阶段预览/概览

elapsedSeconds ────► updateCurrentStage() ─► currentStage* ─► 高亮当前阶段
                                                  │
                                                  └──► wx.vibrateShort()
```

---

## 五、数据结构

### 5.1 参数配置

| 字段 | 类型 | 说明 | 可选值 |
|------|------|------|--------|
| `methodIndex` | number | 冲煮方式 | 手冲(0) / 冷萃(1) |
| `beansIndex` | number | 咖啡品种 | 埃塞俄比亚(0) ~ 其他(6) |
| `roastIndex` | number | 烘焙程度 | 浅度(0) / 中度(1) / 深度(2) |
| `grindIndex` | number | 研磨度 | 面粉状(0) ~ 粗砂糖(4) |
| `weight` | number | 咖啡粉量(g) | 默认 15，手动输入 |
| `pourStyleIndex` | number | 注水手法 | 三段式(0) / 一刀流(1) / 十段法(2) |

### 5.2 联动计算规则

| 冲煮方式 | 烘焙程度 | 水温 (°C) | 粉水比 |
|----------|----------|-----------|--------|
| 冷萃 | — | 5 | 1:12 |
| 手冲 | 浅度 | 93 | 1:16 |
| 手冲 | 中度 | 90 | 1:15 |
| 手冲 | 深度 | 85 | 1:14 |

> 总水量 = 粉量 × 粉水比（四舍五入取整）

### 5.3 注水阶段 (PourStage)

```typescript
interface PourStage {
  name: string;          // 阶段名称（如 "第1段 · 焖蒸"）
  startSeconds: number;  // 起始秒数
  endSeconds: number;    // 结束秒数（Number.MAX_SAFE_INTEGER 表示持续到手动结束）
  targetWater: number;   // 目标注水量 (g)
  hint: string;          // 操作提示
}
```

### 5.4 三种注水手法详解

#### 三段式
| 阶段 | 时间 | 目标水量 | 操作 |
|------|------|----------|------|
| 第1段 · 焖蒸 | 0:00 – 0:30 | W × 2 | 缓慢注水，充分浸润咖啡粉 |
| 第2段 · 主注水 | 0:30 – 1:10 | T × 0.6 | 由中心向外画圈注水 |
| 第3段 · 收尾 | 1:10 – 结束 | T | 保持水位，等待滴滤完成 |

#### 一刀流
| 阶段 | 时间 | 目标水量 | 操作 |
|------|------|----------|------|
| 第1段 · 焖蒸 | 0:00 – 0:30 | W × 2 | 缓慢注水，充分浸润咖啡粉 |
| 第2段 · 中心注水 | 0:30 – 结束 | T | 保持中心缓慢注水至目标水量 |

#### 十段法
| 阶段 | 时间 | 目标水量 |
|------|------|----------|
| 第1段 | 0:00 – 0:20 | T × 1/10 |
| 第2段 | 0:20 – 0:40 | T × 2/10 |
| ... | ... | ... |
| 第10段 | 3:00 – 结束 | T × 10/10 |

> W = 粉量 (g)，T = 总水量 (g)

---

## 六、核心功能实现

### 6.1 联动响应

用户修改冲煮方式、烘焙程度或粉量时，`recalculate()` 函数根据规则表查表得出水温与粉水比，再计算总水量。修改手法时，`recalculateStages()` 基于当前粉量和总水量动态构建阶段数组。

### 6.2 计时器

- 使用 `setInterval` 每秒更新 `elapsedSeconds`
- 格式化输出 `mm:ss`（`formatTime`）
- 组件销毁时（`lifetimes.detached`）自动清除定时器，防止内存泄漏

### 6.3 阶段检测与震动

`updateCurrentStage()` 从后往前遍历阶段数组，匹配当前秒数所属阶段。与 `currentStageName` 比较，若不同则判定为新阶段，触发 `wx.vibrateShort({ type: 'heavy' })` 强震动提醒。

### 6.4 屏幕常亮

开始冲煮时调用 `wx.setKeepScreenOn({ keepScreenOn: true })`，结束或退出时恢复 `false`。

---

## 七、UI 设计规范

### 7.1 色彩系统

| 用途 | 色值 | 说明 |
|------|------|------|
| 导航栏背景 | `#4A3018` | 深咖啡色 |
| 主色调 | `#6F4E37` | 咖啡棕 |
| 辅助色 | `#8D6E63` | 浅咖啡 |
| 背景色 | `#FAF7F2` | 奶油白 |
| 卡片背景 | `#FFFFFF` | 纯白 |
| 强调文字 | `#F5F5DC` | 米黄（渐变卡片上） |
| 正文色 | `#3E2723` | 深褐 |
| 危险/结束 | `#C0392B` | 暗红 |

### 7.2 布局

- 全局采用 Flex 弹性布局
- 卡片式设计：`border-radius: 20rpx`，微阴影 `rgba(74,48,24,0.06)`
- 配置页为垂直滚动 `scroll-view`
- 计时页为全屏居中固定布局

### 7.3 关键尺寸

| 元素 | 尺寸 |
|------|------|
| 计时器字体 | 120rpx |
| 目标水量数字 | 72rpx |
| 卡片圆角 | 20rpx |
| 按钮高度 | 96rpx |
| 开始按钮宽度 | 400rpx |

---

## 八、边界情况处理

| 场景 | 处理方式 |
|------|----------|
| 粉量输入为空或非数字 | `parseFloat` 校验，`isNaN` 或 `<=0` 时忽略输入 |
| 所有阶段均已超时 | 停留在最后一个阶段，持续显示最终目标水量 |
| 页面销毁时定时器仍在运行 | `lifetimes.detached` 中清除 `timerInterval` |
| 冷萃模式下修改烘焙度 | 冷萃水温/比例固定为 5°C / 1:12，不受烘焙度影响 |
| 手法切换 | 切换手法立即重算阶段并更新预览 |

---

## 九、运行方式

1. 打开**微信开发者工具**
2. 导入项目目录 `D:\Program\CoffeeMaker`
3. AppID 已配置为 `wx133f61b555aaf76a`
4. 点击「编译」后即可在模拟器或真机预览

---

## 十、后续扩展建议

- [ ] 增加「配方保存」功能，使用 `wx.setStorageSync` 持久化常用参数组合
- [ ] 增加冲煮历史记录页面
- [ ] 支持自定义注水手法（用户自建分段规则）
- [ ] 增加音效提醒（`wx.createInnerAudioContext`）作为震动的补充
- [ ] 适配深色模式
- [ ] 国际化（中/英/日）
